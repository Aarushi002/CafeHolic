# CafeHolic

## Deploy on Vercel

1. **Root Directory (required):** In Vercel → Project → Settings → General, set **Root Directory** to `htmlcodes`. (Leave empty = broken images and wrong paths.)
2. **Build:** Override Build Command (leave empty). Set **Output Directory** to `.` or leave default.
3. Redeploy after changing Root Directory.